print(max(2, 3, 4)) # 최대 값을 반환한다
print(min(2, 3, 4)) # 최소값을 반환한다
print(round(3.51)) # 가장 가까운 정수로 근사한다. 반올림
print(round(3.4))  # 가장 가까운 정수로 근사한다. 반올림
print(abs(-3)) # 절대값을 반환한다.
print(pow(2, 3)) # 2의 3승